// Set specific frequency and phase
setFrequency(1000);  // 1kHz
setPhase(90);        // 90 degrees phase shift

// Use different phase angles
setPhase(0);    // 0 degrees
setPhase(180);  // 180 degrees
setPhase(270);  // 270 degrees
